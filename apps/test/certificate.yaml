apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: express-test-tls
  namespace: express-test
spec:
  secretName: express-test-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - test-site.rs.nirmalsavinda.online