apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: express-test-server-mtls
  namespace: default
spec:
  selector:
    matchLabels:
      app: express-test-server
  mtls:
    mode: STRICT